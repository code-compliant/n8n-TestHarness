{
  "name": "n8n-testharness-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Story 1.1 intake and classification implementation for n8n test harness.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "test": "node --import tsx --test test/**/*.spec.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
