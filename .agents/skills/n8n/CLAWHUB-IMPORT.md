Imported from ClawHub:

- Page: https://clawhub.ai/thomasansems/n8n
- Downloaded version: 2.0.0
- Imported on: 2026-03-19

Notes:

- ClawHub marked this skill `Benign` with `high confidence` on the page at import time.
- The package includes Python scripts under `scripts/`.
- The scripts appear to call only the configured `N8N_BASE_URL` via the n8n REST API.
- Upstream packaging does not declare Python dependencies, but the scripts import `requests`.

